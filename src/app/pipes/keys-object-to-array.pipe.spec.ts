import { KeysObjectToArrayPipe } from './keys-object-to-array.pipe';

describe('KeysObjectToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new KeysObjectToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
