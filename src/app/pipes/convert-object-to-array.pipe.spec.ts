import { ConvertObjectToArrayPipe } from './convert-object-to-array.pipe';

describe('ConvertObjectToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertObjectToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
