import { TranslatefzPipe } from './translatefz.pipe';

describe('TranslatefzPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatefzPipe();
    expect(pipe).toBeTruthy();
  });
});
